#-------------------------------------------------------------------------------
# Script that returns a result which may be used by other scripts.
#-------------------------------------------------------------------------------
from cake.tools import script

script.setResult(
  library='path/to/library',
  module='path/to/module',
  )
